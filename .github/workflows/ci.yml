name: CI

on:
  push:
    branches: [master]
  pull_request:
  workflow_dispatch:
  # Reused by release.yml so a release runs exactly the same gate as a PR.
  workflow_call:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  checks:
    name: Lint, build & test (Node ${{ matrix.node }})
    runs-on: ubuntu-latest
    timeout-minutes: 15
    strategy:
      fail-fast: false
      matrix:
        # Lower bound is package.json#engines.node; upper bound is what n8n ships today.
        node: ['20.x', '22.x']

    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node }}
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Lint
        run: pnpm lint

      - name: Check formatting
        run: pnpm format:check

      - name: Typecheck
        run: pnpm typecheck

      - name: Build
        run: pnpm build

      - name: Unit tests
        run: pnpm test:unit

      # Runs against dist/, so it has to come after the build.
      - name: Package contract tests
        run: pnpm test:package

      # The same stricter rule set `prepublishOnly` runs at release time, so a release
      # cannot fail on a lint rule CI never checked.
      - name: Prepublish lint
        run: pnpm lint:prepublish
