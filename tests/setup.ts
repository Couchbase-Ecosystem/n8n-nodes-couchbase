// Setup file for Vitest tests
// n8n-workflow is mocked via vitest.config.ts alias

// Global test setup can go here
beforeAll(() => {
	// Any global setup
});

afterAll(() => {
	// Any global cleanup
});
